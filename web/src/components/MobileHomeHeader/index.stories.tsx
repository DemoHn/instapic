import React from 'react'
import { storiesOf } from '@storybook/react'
import MobileHomeHeader from './index'

storiesOf('MobileHomeHeader', module).add('default (mobile)', () => <MobileHomeHeader />)
